# 최상위에 있는 파일로 명시
root = true

# 모든 파일에 적용
[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
indent_style = space
indent_size = 2
trim_trailing_whitespace = true

# '.js'과 '.ts'로 끝나는 파일에 적용
[*.{js,ts,tsx}]
trim_trailing_whitespace = true

# '.json' file
[*.{.json}]
